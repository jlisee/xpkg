name: ncurses
description: Terminal handling library.
version: 5.9

# TODO: make the wide char version of this

build-dependencies:
  - autotools-build
  - tl:c-compiler
  - tl:linker

dependencies:
 - tl:libc

files:
  md5-8cb9c412e5f2d96bc6f459aa8c6282a1:
    url: http://ftp.gnu.org/pub/gnu/ncurses/ncurses-5.9.tar.gz

configure:
  ./configure --prefix=%(prefix)s --includedir=%(prefix)s/include

build:
  make -j%(jobs)s

install:
  make install
