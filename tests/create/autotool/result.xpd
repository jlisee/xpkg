name: autotool
version: 3.2.1
description:

build-dependencies:
  - autotools-build
  - tl:c-compiler
  - tl:linker

dependencies:
 - tl:libc

files:
  hash_replace:
    url: file_replace

configure:
  ./configure --prefix=%(prefix)s

build:
  make -j%(jobs)s

install:
  make install
